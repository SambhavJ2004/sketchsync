# Copy to `.env` and adjust as needed. NEVER commit real secrets.
# The web app runs on port 3000 (Next.js default).

# apps/api (Express HTTP server)
API_PORT=3001

# apps/realtime (ws WebSocket server)
REALTIME_PORT=3002
